function Dropdown(props) {
  const setSelectFamily = (props) => setSelectFamily;

  return (
    <div>
      <li onClick={props.setSelectFamily}>호정이네 가족 </li>
    </div>
  );
}

export default Dropdown;
